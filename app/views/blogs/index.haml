%p#notice= notice
%h1 Blogs
%table
  %thead
    %tr
      %th Title
      %th Body
      %th{:colspan => "3"}
  %tbody
    - @blogs.each do |blog|
      %tr
        %td= blog.title
        %td= blog.body
        %td= link_to 'Show Post', blog
        %td= link_to 'Edit Post', edit_blog_path(blog)
        %td= link_to 'Delete Post', blog, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Blog', new_blog_path
